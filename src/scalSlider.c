#include "scalSlider.h"

void fscale1(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider1 = v; 
  statusprint("Text Slider", data); 
  
}

void fscale2(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider2 = v;  
  
}

void fscale3(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider3 = v;  
  
}

void fscale4(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider4 = v;  
  
}

void fscale5(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider5 = v;  
  
}


void fscale6(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider6 = v;  
  
}

void fscale7(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider7 = v;  
  
}

void fscale8(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider8 = v;  
  
}

void fscale9(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider9 = v;  
  
}

void fscale10(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider10 = v;  
  
}


void fscale11(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider11 = v;  
  
}

void fscale12(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider12 = v;  
  
}

void fscale13(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider13 = v;  
  
}

void fscale14(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider14 = v;  
  
}

void fscale15(GtkWidget *widget, gpointer data){
  VApp* tmp = (VApp*)data;
  double v;
  v =  gtk_range_get_value (GTK_RANGE(widget)); 
  tmp->scale.slider15 = v;  
  
}